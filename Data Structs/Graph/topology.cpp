#include<iostream>
#inlcude<ALGraph.cpp>
#include<Stack.cpp>
using namespace std;

bool TopologicalSort(LGraph lg) {
	initStack(s);
	for(int i=0; i<lg.Vertices; ++i) {
		if(lg.A[i] == 0)
	}
} 
